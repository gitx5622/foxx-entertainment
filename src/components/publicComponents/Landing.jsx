import React, {Fragment} from 'react';
import { Helmet } from 'react-helmet';

const Landing = () => {
    return (
        <Fragment>
            <Helmet>

            </Helmet>

        </Fragment>
    )
};

export default Landing;